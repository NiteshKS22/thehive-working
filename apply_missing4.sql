INSERT INTO cases (tenant_id, case_id, title, description, severity, status, created_by, created_at, updated_at, entity_version) VALUES ('dev-tenant', '33333333-3333-3333-3333-333333333333', 'E2E Test Case for Tasks (Dev Tenant)', 'This case should be visible because DEV_MODE overrides all auth to use dev-tenant.', 2, 'OPEN', 'admin', extract(epoch from now()) * 1000, extract(epoch from now()) * 1000, 1) ON CONFLICT DO NOTHING;
